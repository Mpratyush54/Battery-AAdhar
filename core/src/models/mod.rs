pub struct Battery {
    pub bpan: String,
}

pub struct BatteryIdentifier {
    pub id: String,
}

pub struct BatteryDescriptor {
    pub id: String,
}

pub struct Stakeholder {
    pub id: String,
}
