version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: rides_user
      POSTGRES_PASSWORD: rides_pass
      POSTGRES_DB: rides_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  core:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      # Override the DB URL to point to the `db` service on port 5432
      DATABASE_URL: postgres://rides_user:rides_pass@db:5432/rides_db
      RUST_LOG: info
      ENCRYPTION_KEY: "01234567890123456789012345678901"
    ports:
      - "50051:50051"
    depends_on:
      - db

volumes:
  postgres_data:
